# Web Research Workflow

When external web evidence is needed:

1. Prefer `web_search` to discover relevant sources.
2. Use `web_fetch` when you already have a concrete URL or need to read one result in detail.
3. Prefer concise, high-signal evidence over broad browsing.
4. If web tools are unavailable or return weak evidence, say so clearly.
