# Memory Recall Workflow

When prior preferences, risk constraints, watchlists, exclusions, or earlier conclusions may matter:

1. Use `memory_search` first with a focused query.
2. If the result is relevant, use `memory_get` to read only the needed snippet.
3. Keep cited memory snippets small and relevant.
4. Do not assume memory is complete; if recall is missing, say so.
